# ── Supabase ─────────────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# ── Anthropic (chart interpretations) ────────────────────
# Without this the app still works; the interpretation panel
# shows "به‌زودی" instead of failing.
ANTHROPIC_API_KEY=
